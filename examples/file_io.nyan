fetch "file"

# Read entire file as a single string
nyan content = file.snoop("examples/data.txt")
nya(content)

# Read file line by line and print with prefix
file.stalk("examples/data.txt") |=| lick(paw(line) { "=> " + line }) |=| nya
