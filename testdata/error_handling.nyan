# Error handling functions

meow divide(a int, b int) int {
  sniff (b == 0) {
    hiss("division by zero")
  }
  bring a / b
}

meow risky_chain(x int) int {
  sniff (x < 0) {
    hiss("negative input")
  }
  bring x * 2
}
